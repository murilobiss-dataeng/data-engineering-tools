# Monorepo: use `.github/workflows/main.yml` na raiz do repositório **data-engineering-tools**.

name: WhatsApp Bot — mb-ofertas (use raiz do repo)

on:
  workflow_dispatch:

jobs:
  hint:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice::Na raiz do repo, rode o workflow **WhatsApp Bot (postar ofertas)** (`.github/workflows/main.yml`)."
