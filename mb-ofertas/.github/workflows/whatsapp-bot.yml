# Monorepo: workflows canônicos estão em `.github/workflows/whatsapp-channel-*.yml` na raiz.

name: WhatsApp Bot — mb-ofertas (use raiz do repo)

on:
  workflow_dispatch:

jobs:
  hint:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice::Abra Actions na raiz **data-engineering-tools**: workflows **WhatsApp — health/tech/ofertas/faith** ou **WhatsApp — todos os canais (manual)**."
